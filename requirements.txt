# Web Framework & Server
fastapi>=0.100.0
uvicorn[standard]>=0.23.0

# Templating & Static Files
jinja2>=3.1.0

# Database & ORM
sqlmodel>=0.0.14
# Note: sqlite3 is built-in to Python, no pip install needed

# Background Tasks & Scheduling
apscheduler>=3.10.0

# AI / Gemini
google-genai>=1.10.0
python-dotenv>=1.0.1
perigon>=1.0.0
requests>=2.31.0

# Testing
pytest>=7.0.0
httpx>=0.24.0

# Hardware dependencies live in requirements-hardware.txt
# so local/dev installs (macOS/Windows/Linux without GPIO) still work.